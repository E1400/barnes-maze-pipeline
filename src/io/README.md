# `src/io` — export and project files

CSV/XLSX export (SheetJS) and the versioned project-file JSON schema.

Both formats embed the parameters used and the tool version — an exported
number that can't be traced back to the settings that produced it isn't
publishable.
